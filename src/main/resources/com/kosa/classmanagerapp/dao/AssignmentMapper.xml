<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kosa.classmanagerapp.dao.AssignmentMapper">
    <insert id="save" parameterType="Assignment">
        INSERT INTO assignment (title, content,
        creator_id, assignment_type, team_id,
        is_close, presentation_order_team_id,
        due_date)
        VALUES (#{title}, #{content},
        #{creatorId}, #{assignmentType}, #{teamId},
        #{isClose}, #{presentationOrderTeamId},
        #{dueDate});
    </insert>
</mapper>