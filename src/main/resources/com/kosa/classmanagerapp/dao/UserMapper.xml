<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kosa.classmanagerapp.dao.UserMapper">
    <select id="findByUserName" resultType="User">
        SELECT * FROM user WHERE user_name = #{userName}
    </select>
    <insert id="save" parameterType="User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (user_name, password_hash, full_name, birthday, authorization)
        VALUES (#{userName}, #{passwordHash}, #{fullName}, #{birthday}, 'USER')
    </insert>
    <update id="updateTeam" parameterType="User">
        UPDATE user
        SET team_id = #{teamId}
        WHERE id = #{id}
    </update>

</mapper>